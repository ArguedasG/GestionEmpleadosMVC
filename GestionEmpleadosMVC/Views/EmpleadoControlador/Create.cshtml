@model Empleado

<div class="container mt-5">
    <div class="card shadow-lg p-4 rounded">
        <h2 class="text-center text-primary mb-4">Insertar Empleado</h2>
        @if (ViewBag.Error != null)
        {
            <div class="alert alert-danger text-center">@ViewBag.Error</div>
        }
        else if (ViewBag.Message != null)
        {
            <div class="alert alert-success text-center">@ViewBag.Message</div>
        }

        <form method="post">
            <div class="mb-3">
                <label class="form-label fw-bold">Nombre:</label>
                <input type="text" name="Nombre" class="form-control" required pattern="[A-Za-z -]+" placeholder="Ingrese el nombre" />
            </div>

            <div class="mb-3">
                <label class="form-label fw-bold">Salario:</label>
                <input type="text" name="Salario" class="form-control" required
                       inputmode="decimal" pattern="\d+(\.\d{1,2})?"
                       placeholder="Ingrese el salario" oninput="validarNumero(this)" />
            </div>

            <div class="d-grid">
                <button type="submit" class="btn btn-primary">Insertar</button>
            </div>
        </form>

        <div class="text-center mt-3">
            <a href="/EmpleadoControlador/Index" class="btn btn-outline-secondary">Regresar</a>
        </div>
    </div>
</div>

<script>
    function validarNumero(input) {
        input.value = input.value.replace(/[^0-9.]/g, 
        if ((input.value.match(/\./g) || []).length > 1) {
            input.value = input.value.slice(0, -1);
        }
    }
</script>
